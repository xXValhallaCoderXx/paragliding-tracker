// Consumed by Expo's Metro CSS transform worker, which only probes for
// postcss.config{.mjs,.js,.json} — a .cjs or .ts file here would be ignored silently.
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
